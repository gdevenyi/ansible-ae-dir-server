<h1>{text_heading}</h1>

{text_error}

<h2>Normal use</h2>
<p>
  <a href="/web2ldap?{{ openldap_ldapi_uri }}/{{ aedir_suffix }}????bindname=">Local &AElig;-DIR via LDAPI</a>
  (if in doubt, click this!)
</p>

<h2>&AElig;-DIR providers</h2>
<table>
{% for host in aedir_provider_hosts %}
{% set replica = hostvars[host]['openldap_service_fqdn'] -%}
  <tr>
    <td>
      <a href="/web2ldap?ldaps://{{ replica }}/{{ aedir_suffix }}????bindname=">{{ replica }}</a>
    </td>
    <td>
      {{ host }}
    </td>
  </tr>
{% endfor %}
</table>

<h2>&AElig;-DIR consumers</h2>
<table>
{% for host in aedir_consumer_hosts %}
{% set replica = hostvars[host]['openldap_service_fqdn'] -%}
  <tr>
    <td>
      <a href="/web2ldap?ldaps://{{ replica }}/{{ aedir_suffix }}????bindname=">{{ replica }}</a>
    </td>
    <td>
      {{ host }}
    </td>
  </tr>
{% endfor %}
</table>
