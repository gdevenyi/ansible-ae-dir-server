<h1>{text_heading}</h1>

{text_error}

<p>
  <a class="CommandLink"
    href="/web2ldap/searchform?{{ openldap_ldapi_uri }}/{{ aedir_suffix }}????bindname=">
    Login to &AElig;-DIR
  </a>
  (if in doubt, click this!)
</p>

<h2>Documentation</h2>
<ul>
  <li>
    <a href="/web2ldap.html">web2ldap user guide</a>
  </li>
</ul>

<h2>Other apps</h2>
<ul>
  <li>
    <a href="/pwd">Password Self-Service</a>
  </li>
{% if oath_ldap_enabled==True %}
  <li>
    <a href="/oath">OATH-LDAP Token Enrollment Service</a>
  </li>
{% endif %}
</ul>

<h2>Admin/auditor short cuts</h2>
<table>
  <tr>
    <th>Task</th>
    <th>Admin Role(s)</th>
  </tr>
  <tr>
    <td>
      <a class="CommandLink"
         href="/web2ldap?{{ openldap_ldapi_uri }}/{{ aedir_suffix }}??sub?(&(objectClass=aeUser)(aeStatus=0)(!(userPassword=*)))?bindname=">
        Search active personal user accounts without password
      </a>
    </td>
    <td>
      &AElig; admins/auditors, Zone admins
    </td>
  </tr>
  <tr>
    <td>
      <a class="CommandLink"
         href="/web2ldap?{{ openldap_ldapi_uri }}/{{ aedir_suffix }}??sub?(&(objectClass=aeObject)(aeStatus=0)(aeNotAfter<={value_currenttime}))?bindname=">
        Search expired entries still active
      </a>
    </td>
    <td>
      &AElig; admins/auditors, Zone admins
    </td>
  </tr>
  <tr>
    <td>
      <a class="CommandLink"
         href="/web2ldap?{{ openldap_ldapi_uri }}/{{ aedir_suffix }}??sub?(objectClass=msPwdResetObject)?bindname=">
        Search on-going password resets
      </a>
    </td>
    <td>
      &AElig; admins/auditors, Zone admins
    </td>
  </tr>
  <tr>
    <td>
      <a class="CommandLink"
         href="/web2ldap/searchform?{{ openldap_ldapi_uri }}/{{ aedir_accesslog_suffix }}????bindname=">
        Search access log
      </a>
    </td>
    <td>
      &AElig; admins/auditors
    </td>
  </tr>
</table>

<h3>&AElig;-DIR remote servers</h3>
{% for role in ['provider', 'consumer'] %}
<h4>{{ role }}(s)</h4>
<table>
  <tr>
    <th>Service</th>
    <th>Host</th>
    <th>Id</th>
  </tr>
{% for host in aedir_hosts[role] %}
{% set replica = hostvars[host].get('openldap_service_fqdn',host) -%}
{% set server_id = hostvars[host].get('openldap_server_id','') -%}
  <tr>
    <td>
      <a class="CommandLink" href="/web2ldap/searchform?ldaps://{{ replica }}/{{ aedir_suffix }}????bindname=">{{ replica }}</a>
    </td>
{% if replica!=host %}
    <td>
      {{ host }}
    </td>
{% else %}
    <td>
      -
    </td>
{% endif %}
    <td>
      {{ server_id }}
    </td>
  </tr>
{% endfor %}
</table>

{% endfor %}

<h2>&AElig;-DIR configuration</h2>
<table>
  <tr>
    <td>Service FQDN:</td>
    <td>{{ openldap_service_fqdn }}</td>
  </tr>
  <tr>
    <td>Hostname:</td>
    <td>{{ ansible_fqdn }}</td>
  </tr>
  <tr>
    <td>base DN / search root:</td>
    <td>{{ aedir_suffix }}</td>
  </tr>
  <tr>
    <td>POSIX-UID number range:</td>
    <td>{{ aedir_min_uid }}..{{ aedir_max_uid }}</td>
  </tr>
  <tr>
    <td>POSIX-GID number range:</td>
    <td>{{ aedir_min_gid }}..{{ aedir_max_gid }}</td>
  </tr>
</table>
