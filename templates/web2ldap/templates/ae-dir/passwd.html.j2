<h1>{text_heading}</h1>
<p>{text_msg}</p>
{form_begin}
{value_dn}
{value_passwd_action}
{value_passwd_who}
<table summary="Password input form">
<tr>
  <td>{text_desc}</td>
  <td>{text_whoami}</td>
</tr>
<tr>
  <td>Enter new password</td>
  <td>
    <input
      type="password"
      name="passwd_newpasswd"
      maxlength="100"
      size="40"
      title="The new password to be set"
      value="">
  </td>
</tr>
<tr>
  <td>Repeat new password</td>
  <td>
    <input
      type="password"
      name="passwd_newpasswd"
      maxlength="100"
      size="40"
      title="Again the new password for verification"
      value="">
  </td>
</tr>
{disable_ownuser_start}
<tr>
  <td>Force password change</td>
  <td><input type="checkbox" name="passwd_forcechange" value="yes"></td>
</tr>
<tr>
  <td>Display password change link</td>
  <td><input type="checkbox" name="passwd_inform" value="display_url"></td>
</tr>
{disable_ownuser_end}
{disable_clientsidehashing_start}
  <tr>
    <td>Password encryption method</td>
    <td>{form_field_passwd_scheme}</td>
  </tr>
{disable_clientsidehashing_end}
{disable_syncnthash_start}
  <tr>
    <td>Sync with NtPassword attribute (for Samba)</td>
    <td>{form_field_passwd_ntpasswordsync}</td>
  </tr>
{disable_syncnthash_end}
{disable_settimesync_start}
  <tr>
    <td>Sync password setting times</td>
    <td>{form_field_passwd_settimesync}</td>
  </tr>
{disable_settimesync_end}
</table>

<input type="submit" value="Change Password">
<input type="reset" value="Reset">
</form>

<p>
    If you leave the password field empty a new password is
    automatically generated and displayed in clear-text on the
    screen!
</p>

<h2>Security guide lines</h2>

<ul>
  <li>
    Passwords for privileged user accounts must be at least
    <strong>12</strong> characters long.
  </li>
  <li>
    Passwords must not be used for any other account.
  </li>
  <li>
    Passwords for fall-back use must be at least
    <strong>16</strong> characters long.
  </li>
  <li>
    You have to set a complex password with at least
    <strong>5</strong> characters which are numbers or special chars.
  </li>
  <li>
    You must not use passwords guessable by dictionary attacks.
  </li>
  <li>
    Take care that you enter the password only while nobody is
    watching your keyboard.
  </li>

</ul>
